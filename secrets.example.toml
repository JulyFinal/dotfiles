# Copy this file to secrets.toml and keep it local with mode 0600.
#
# Add only values that are referenced by a real .j2 template. Keep the section
# name aligned with that template, for example:
#
# [service]
# api_token = "replace-me"
