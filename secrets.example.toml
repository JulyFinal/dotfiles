# Copy this file to secrets.toml, replace the placeholders, and keep it local.
# secrets.toml is ignored by Git and should be mode 0600.

[mihomo]
subscription_url = "https://example.invalid/subscription"
controller_secret = "changeme"
