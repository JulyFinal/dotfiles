# Generated entrypoint: keep personal, machine-only additions in ~/.bashrc.local.
[ -f "$HOME/.config/shell/common.sh" ] && . "$HOME/.config/shell/common.sh"
[ -f "$HOME/.bashrc.local" ] && . "$HOME/.bashrc.local"
