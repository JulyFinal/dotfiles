# Route Chrome's complete network stack (including WebAuthn hybrid transport)
# through the local v2rayN/Xray mixed HTTP+SOCKS listener.
--proxy-server=http://127.0.0.1:10808

# Slightly denser Chrome UI on the 1.25x niri output.
--force-device-scale-factor=1.10
